# 三场景剩余差距

1. **脱丁烷塔数值主链未完成验收。** 本地可用 raw_samples 数据是归一化字段样本，物理温度字段契约未通过，缺 bottom_temperature_a、bottom_temperature_b、top_temperature、tray6_temperature。需要有来源/授权的物理量数据，或可核验的逆缩放及采样元数据。未增加绕过别名、放宽范围或生成 mock。
2. **干燥器是已有合成验收集。** 可验证工程链路，但不代表工业现场泛化性能。本次没有严格优质动态段，测试 RMSE 未优于 persistence，因此 partial。
3. **高炉部分字段 SNR 不可估计。** null 及其原因原样保留，不能以零值替代。模型诊断 success 只表示既有门禁满足；残差只有 ACF 诊断，未完成正式白噪声检验，不等于生产准入。
4. **选择出的模型可为 AR。** 共用 AR/ARX 搜索按既有验证 BIC、稳定性与仿真门禁选择；不能为展示 ARX 字样强制改选。两份可运行数据这次均选 AR。
5. **旧通用 pipeline 编排仍保留。** 新原始文件适配入口已走 MD DAG，现有完整上传流程未全部切换为该入口；未复制数值算法。CLI 入口的运行记录位于 agent_skill_runs/scene_inputs，未作为前端最近 pipeline run 自动发布。
6. **多输出未扩展为联合 MIMO 识别。** 本阶段由 field_roles.target 指定单一主目标，其余受控量保留为场景上下文/清洗字段。
7. **文档召回仍是词项匹配。** intent_terms 仅约束候选范围，不能当作完整自然语言理解；本阶段不重写第一阶段 Resolver。
8. **前端验证边界。** 自动测试和生产构建通过；本阶段没有新的三场景浏览器逐页视觉验收，没有调整 3D 或界面布局。

结论：Skill 接口和算法实现已共用；当前证据不足以宣称三个场景都完成真实现场数据的完整辨识验收。
