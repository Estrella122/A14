# ProcessPilot A14 接口说明

默认后端地址为 `http://127.0.0.1:8000`，前端开发服务会将 `/api` 请代理到后端。返回值均为 JSON，文件下载接口除外。

## 健康检查

```http
GET /api/
```

用于判断后端和闭环寻优流程是否可用。

## 上传并执行流水线

```http
POST /api/pipeline/runs/
Content-Type: multipart/form-data
```

字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `file` | 是 | CSV 文件 |
| `scenario_id` | 否 | 默认 `auto`，也可指定场景 ID |
| `instruction` | 否 | Agent 任务说明 |
| `resample_rule` | 否 | 重采样周期，例如 `5s` |
| `max_lag` | 否 | 最大时滞搜索点数 |

成功后返回任务快照，包含 `run_id`、`stages`、`results` 和 `artifacts`。

## 读取任务

```http
GET /api/pipeline/runs/latest/
GET /api/pipeline/runs/{run_id}/
```

第一个接口返回最近任务，第二个接口按 `run_id` 返回指定任务。

## 重新执行

```http
POST /api/pipeline/runs/{run_id}/rerun/
Content-Type: application/json
```

可传入 `resample_rule` 和 `max_lag`。重跑会保留原始 CSV，并生成新的任务 ID 和独立产物目录。

## Agent 对话

```http
POST /api/agent/chat/
Content-Type: application/json
```

请求示例：

```json
{
  "message": "说明当前数据质量和闭环寻优结论",
  "run_id": "20260828_120000_abcd1234"
}
```

Agent 会基于该任务的真实证据返回意图、回答、卡片、日志和建议问题。

## 产物下载

```http
GET /api/pipeline/runs/{run_id}/artifacts/{artifact_key}/
```

常用 `artifact_key`：

| 值 | 产物 |
| --- | --- |
| `standardized_csv` | 标准化数据 |
| `mapping_csv` | 字段映射报告 |
| `cleaned_csv` | 清洗后数据 |
| `segments_csv` | 动态段结果 |
| `metrics_json` | 系统辨识指标 |
| `optimization_json` | 闭环寻优记录 |
| `review_json` | Agent 评审结果 |
| `analysis_report_md` | Markdown 分析报告 |

## 模块证据

```http
GET /api/integration/
GET /api/integration/{module_key}/
```

`module_key` 支持 `standardization`、`cleaning`、`modeling` 和 `agent`。
