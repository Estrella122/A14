# ProcessPilot A14 使用说明

## 1. 运行环境

- Windows 10/11、macOS 或常见 Linux 发行版
- Python 3.12 或更高版本
- Node.js 20.19+ 或 22.12+
- 首次安装需要连网下载 Python 和 Node.js 依赖

## 2. 首次安装

在工程根目录打开 PowerShell、终端或命令行：

```bash
npm run setup
```

安装器会自动创建 `.venv`，安装后端依赖和前端依赖，并执行 Django 配置检查。

## 3. 启动系统

```bash
npm run dev
```

启动成功后访问：

- Agent 中枢：<http://127.0.0.1:5176/agent-review/>
- 项目驾驶舱：<http://127.0.0.1:5176/overview/>
- 后端 API：<http://127.0.0.1:8000/api/>

按 `Ctrl+C` 停止前后端服务。

## 4. 演示数据

钢铁高炉主演示文件位于：

```text
frontend/public/datasets/blast_furnace_real_720h.csv
```

工业干燥器合成验收文件位于：

```text
演示数据/工业干燥器_10秒_867条_3输入3输出_合成验收数据.csv
```

在 Agent 中枢点击“上传CSV并全流程运行”，选择其中一个文件。系统会从场景识别开始，依次执行字段统一、清洗优选、时滞辨识、闭环寻优、Agent 评审和报告生成。炼油脱丁烷精馏塔因上游公开副本的再分发许可证尚不明确，仓库仅保留适配器和来源说明，取得合法数据副本后再导入。

高炉 CSV 是从 CC BY 4.0 公开真实数据派生的演示切片；工业干燥器 CSV 是根据工业机理生成的合成验收数据，不宣称来自真实工厂。

## 5. 局域网访问

macOS/Linux：

```bash
PROCESSPILOT_HOST=0.0.0.0 npm run dev
```

Windows PowerShell：

```powershell
$env:PROCESSPILOT_HOST="0.0.0.0"
npm run dev
```

同一局域网内的设备可通过 `http://主机IP:5176/` 访问。如无法访问，需允许系统防火墙放行 `5176` 和 `8000` 端口。

## 6. 常用命令

```bash
npm run build
npm run test:backend
npm run test:closed-loop
```

## 7. 数据与产物

- 上传的源数据和每轮产物保存在 `runtime/pipeline_runs/`。
- 不使用 MySQL 时，默认使用本地 SQLite 数据库 `apc_agent.db`。
- 2号字段 Agent 的训练数据与训练脚本保留在 `integrations/standardization/training_data/` 和 `integrations/standardization/training/`。
- 交付包不包含开发机的历史任务、用户上传数据、虚拟环境、前端依赖和旧模型版本。
